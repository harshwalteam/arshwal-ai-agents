{
  "name": "harshwal-ai-agents",
  "version": "1.0.0",
  "description": "Harshwal AI Agent Services workpaper. Root package.json for platforms (Vercel, Glitch, etc.) that expect it at the repo root. The /api functions use only Node's built-in fetch, no extra dependencies needed.",
  "engines": {
    "node": ">=18"
  }
}
